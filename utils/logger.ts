const universalLogger = (message: string): string => {
  return ``
}

/**
 * logger
 */
export default class Logger {

  /**
   * universal log
   * @param msg
   */
  info(msg: string) {

  }

  /**
   * error log
   * @param msg
   */
  error(msg: string) {

  }

  /**
   * debug log
   * @param msg
   */
  debug(msg: string) {

  }
}
